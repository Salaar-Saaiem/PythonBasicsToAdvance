'''
min(list_name) anf max(list_name) is used to fetch the smallest and the largest element from the list respectively
'''

num=[1,4,2,65,234,72,-21,34,-72,-1,-0]
print(min(num))
print(max(num))

names=['saaiem','sam','tanishka','tax'] #it compares the first character of each word and returns the smallest and largest word in alphabetical order
print(min(names)) #it will return saaiem bcoz a in the 3rd place of saaiem is smaller than m in the 3rd place of sam
print(max(names))