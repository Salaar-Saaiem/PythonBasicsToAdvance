'''
.clear() is used when u want to delete all the elements from the list and make it empty list, but the list itself will not be deleted, it will be there but empty
.clear() method does not work with tuples because tuples are immutable (cannot be changed after they are created).
'''

str=['Saaiem', 'sam','Salaar']
print(str)
str.clear()
print(str)